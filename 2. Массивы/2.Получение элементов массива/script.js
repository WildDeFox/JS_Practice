// Создайте массив с элементами 1, 2, 3. Выведите на экран каждый из этих элементов.
const arr = [1, 2, 3];
console.log(arr[0], arr[1], arr[2]);


// Выведите на экран сумму элементов этого массива.
console.log(arr[0] + arr[1] + arr[2]);


// Дан следующий массив:
// const arr1 = ['a', 'b', 'c', 'd'];
// Выведите с помощью этого массива следующую строку: 'a+b+c+d'
const arr1 = ['a', 'b', 'c', 'd'];
console.log(`${arr1[0]}+${arr1[1]}+${arr1[2]}+${arr1[3]}`)